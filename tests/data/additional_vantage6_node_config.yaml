# Any configurations provided below are added through to the configuration file using the vantage6 developer network setup.
# algorithm_policies are added dynamically as the name of the to-build algorithm is determined dynamically;
# see 'extra_node_config_file' in confest.py for more details.