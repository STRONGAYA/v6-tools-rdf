# Any configurations provided below are added through to the configuration file using the vantage6 developer network setup.
# This space is intentionally left empty here as no additional store configuration is required for the current test setup.